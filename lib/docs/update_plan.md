# Arcle CLI vNext - Implementation Plan

## Vision

Transform Arcle from a Flutter Clean Architecture project generator into an Agentic Flutter Development Platform that supports multiple AI coding agents while enforcing consistent architecture, coding standards, security practices, and project quality.

---

## Goals

### 1. Upgrade Platform Requirements

#### Current
- Flutter: Older supported versions
- Dart SDK: >=3.5.0

#### New Minimum Requirements
- Flutter: 3.29.3+
- Dart SDK: >=3.7.0 <4.0.0

#### Tasks
- Update generated pubspec.yaml SDK constraints
- Update templates and version validation logic
- Update README documentation
- Add compatibility validation command

---

## 2. Agentic CLI Architecture

### Objective

Allow developers to use their preferred AI coding agent with Arcle by simply providing an API key and configuration.

### Supported Agents

#### Claude Code
- API key configuration
- Project context injection
- Architecture enforcement

#### OpenAI Codex
- API key configuration
- Coding standards integration
- Project awareness

#### Gemini
- API key configuration
- Flutter project context integration
- Architecture rules support

#### Custom Agent
- Custom endpoint support
- Custom model support
- Flexible configuration

### Generated Structure

```text
.ai/
├── settings.yaml
├── project-context.md
├── architecture-rules.md
├── coding-rules.md
├── security-rules.md
├── permissions.yaml
└── prompts/
```

### New Commands

```bash
arcle agent add
arcle agent remove
arcle agent switch
arcle agent list
arcle agent validate
```

---

## 3. Universal Agent Configuration

### Objective

Generate a shared configuration layer that every supported AI agent can understand and follow.

### settings.yaml

Generated by default and fully editable by developers.

Example:

```yaml
architecture:
  clean_architecture: true

state_management:
  type: riverpod

routing:
  type: go_router

localization:
  enabled: true

responsive_ui:
  enabled: true

testing:
  enabled: true

ai:
  auto_generate_tests: true
  auto_generate_docs: true
```

### Benefits

- Consistent AI behavior
- Project-wide standards
- Easy customization
- Agent-independent configuration

---

## 4. Professional Analyzer Configuration

### Objective

Generate strict and production-ready analyzer rules.

### Generated analysis_options.yaml

Includes:

```yaml
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    - always_use_package_imports
    - avoid_print
    - prefer_const_constructors
    - prefer_const_declarations
    - prefer_final_fields
    - prefer_final_locals
    - unnecessary_const
    - unnecessary_new
    - use_super_parameters
    - avoid_unnecessary_containers
    - sized_box_for_whitespace
    - prefer_single_quotes
```

### Additional Improvements

- Dependency ordering
- Constructor ordering
- Trailing comma enforcement
- Package import enforcement
- Flutter best practices

---

## 5. AI Development Standards

### Objective

Provide AI-ready development rules and project governance.

### Coding Rules

Generated file:

```text
.ai/coding-rules.md
```

Contains:

- Naming conventions
- Layer responsibilities
- Widget structure rules
- Repository patterns
- State management conventions
- Testing expectations

### Security Rules

Generated file:

```text
.ai/security-rules.md
```

Contains:

- No secrets in source code
- Secure token handling
- Secure storage requirements
- Logging restrictions
- API protection guidelines
- OWASP recommendations

---

## 6. AI Agent Integrations

### Claude Code

Generated:

```text
.claude/
├── CLAUDE.md
└── settings.json
```

### OpenAI Codex

Generated:

```text
.codex/
├── instructions.md
└── settings.json
```

### Gemini

Generated:

```text
.gemini/
├── GEMINI.md
└── settings.json
```

### Purpose

- Architecture awareness
- Coding standards enforcement
- Consistent project generation
- Better AI outputs

---

## 7. AI Permission System

### Objective

Prevent destructive AI actions and provide controlled project access.

Generated:

```text
.ai/permissions.yaml
```

Example:

```yaml
agents:
  create_files: true
  modify_files: true
  delete_files: false
```

### Benefits

- Safer AI execution
- Reduced accidental project damage
- Standardized behavior

---

## 8. Responsive Dimensions System

### Objective

Generate a unified responsive utility for all state management solutions. 
'''
class Dimensions {
  static double get _screenHeight =>
      ui.PlatformDispatcher.instance.views.first.physicalSize.height /
      ui.PlatformDispatcher.instance.views.first.devicePixelRatio;
  static double get _screenWidth =>
      ui.PlatformDispatcher.instance.views.first.physicalSize.width /
      ui.PlatformDispatcher.instance.views.first.devicePixelRatio;

  // Base design size from Figma (iPhone SE or similar mobile design)
  static const double _mockupHeight = 884.0;
  static const double _mockupWidth = 390.0;

  // Scale factors for responsive design
  static double get _heightRatio => _screenHeight / _mockupHeight;
  static double get _widthRatio => _screenWidth / _mockupWidth;

  // Font scaling considering both screen size and user's text scale preference
  static double get _fontRatio => (_widthRatio + _heightRatio) / 2;

  /// Use for vertical values like height, vertical padding/margin
  static double height(double value) => value * _heightRatio;

  /// Use for horizontal values like width, horizontal padding/margin
  static double width(double value) => value * _widthRatio;

  /// Use for font sizes
  static double font(double value) => value * _fontRatio;

  /// Use for border radius
  static double radius(double value) => value * _widthRatio;

  /// Use for icon sizes
  static double icon(double value) => value * _widthRatio;

  /// Use for border width
  static double border(double value) => value * _widthRatio;

  /// EdgeInsets helpers
  static EdgeInsets paddingAll(double padding) => EdgeInsets.all(width(padding));

  static EdgeInsets paddingHorizontal(double horizontal) =>
      EdgeInsets.symmetric(horizontal: width(horizontal));

  static EdgeInsets paddingVertical(double vertical) =>
      EdgeInsets.symmetric(vertical: height(vertical));

  static EdgeInsets paddingTop(double top) => EdgeInsets.only(top: height(top));

  static EdgeInsets paddingBottom(double bottom) => EdgeInsets.only(bottom: height(bottom));

  static EdgeInsets paddingLeft(double left) => EdgeInsets.only(left: width(left));

  static EdgeInsets paddingRight(double right) => EdgeInsets.only(right: width(right));

  static EdgeInsets paddingSymmetric({
    double horizontal = 0,
    double vertical = 0,
  }) =>
      EdgeInsets.symmetric(
        horizontal: width(horizontal),
        vertical: height(vertical),
      );

  static EdgeInsets paddingOnly({
    double left = 0,
    double top = 0,
    double right = 0,
    double bottom = 0,
  }) =>
      EdgeInsets.only(
        left: width(left),
        top: height(top),
        right: width(right),
        bottom: height(bottom),
      );

  /// Margin helpers
  static EdgeInsets marginAll(double margin) => EdgeInsets.all(width(margin));

  static EdgeInsets marginHorizontal(double horizontal) =>
      EdgeInsets.symmetric(horizontal: width(horizontal));

  static EdgeInsets marginVertical(double vertical) =>
      EdgeInsets.symmetric(vertical: height(vertical));

  static EdgeInsets marginTop(double top) => EdgeInsets.only(top: height(top));

  static EdgeInsets marginBottom(double bottom) => EdgeInsets.only(bottom: height(bottom));

  static EdgeInsets marginLeft(double left) => EdgeInsets.only(left: width(left));

  static EdgeInsets marginRight(double right) => EdgeInsets.only(right: width(right));

  static EdgeInsets marginSymmetric({
    double horizontal = 0,
    double vertical = 0,
  }) =>
      EdgeInsets.symmetric(
        horizontal: width(horizontal),
        vertical: height(vertical),
      );

  static EdgeInsets marginOnly({
    double left = 0,
    double top = 0,
    double right = 0,
    double bottom = 0,
  }) =>
      EdgeInsets.only(
        left: width(left),
        top: height(top),
        right: width(right),
        bottom: height(bottom),
      );
}
'''

### Generated File

```text
lib/core/utils/dimensions.dart
```

### Features

- Width scaling
- Height scaling
- Font scaling
- Radius scaling
- Border scaling
- Icon scaling
- Padding helpers
- Margin helpers

### Supported Architectures

- Riverpod
- Bloc
- GetX
- Provider

### Default Design Reference

- Width: 390
- Height: 884

---

## 9. Project Template Enhancements

### Updated Structure

```text
lib/
├── core/
│   ├── constants/
│   ├── network/
│   ├── services/
│   ├── themes/
│   ├── utils/
│   └── widgets/
│
├── features/
├── routes/
└── main.dart
```

### Improvements

- Cleaner organization
- AI-friendly structure
- Better scalability
- Consistent architecture

---

## 10. Setup & Validation Scripts

Generated:

```text
scripts/
├── setup.sh
├── setup.ps1
├── doctor.sh
└── doctor.ps1
```

### Responsibilities

- Flutter validation
- Dart validation
- Dependency verification
- Agent verification
- Environment setup

---

## 11. CLI Enhancements

### New Commands

```bash
arcle ai init
arcle ai sync
arcle ai validate
arcle ai doctor
```

### Existing Command Improvements

```bash
arcle doctor
arcle validate
arcle upgrade
arcle lint
```

---

## Migration Strategy

### Existing Projects

Command:

```bash
arcle upgrade
```

Actions:

- Upgrade SDK constraints
- Generate .ai directory
- Create analyzer configuration
- Generate dimensions.dart
- Create agent configuration files
- Preserve user code

---

## Release Phases

### Phase 1
- SDK upgrade
- Analyzer improvements
- Responsive dimensions utility

### Phase 2
- Agent configuration system
- AI settings generation
- Security and coding rules

### Phase 3
- Claude integration
- Codex integration
- Gemini integration

### Phase 4
- Custom agent support
- Permission system
- Validation and doctor commands

### Phase 5
- Documentation updates
- Migration tools
- Final testing and release

---

## Success Criteria

### Architecture
- Clean Architecture enforced
- Consistent project generation

### AI
- Claude supported
- Codex supported
- Gemini supported
- Custom agents supported

### Quality
- Production-grade linting
- Strong analyzer rules
- Secure defaults

### Developer Experience
- One-command setup
- AI-ready projects
- Easy customization
- Automated validation

---

## Future Roadmap

- MCP integration
- Multi-agent workflows
- AI feature generation
- Automated code review
- Architecture auditing
- CI/CD generators
- Enterprise project templates
